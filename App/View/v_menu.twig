<nav class="top-bar" data-topbar role="navigation">
    <ul class="title-area">
        <li class="name">
            <h1> <a href="{{ path('manifestant.show') }}">WEB</a></h1>
        </li>
        <li class="toggle-topbar menu-icon">
            <a href="#"><span>Menu</span></a>
        </li>
    </ul>
    <section class="top-bar-section">
        <ul class="left">
            <li class="divider"></li>
            <li><a href="{{ path('manifestant.show') }}"><strong><span class="fi-prohibited"></span> Commandez votre manifestation !</strong></a></li>
            {% if app.session.get('logged') is defined and app.session.get('logged') == 1 %}
                <li class="divider"></li>
                <li><a href="{{ path('panier.show') }}"><span class="fi-shopping-bag"></span> Votre Panier</a></li>

                <li><a href="{{ path('commande.clientList') }}"><span class="fi-shopping-cart"></span> Vos Commandes</a></li>
                <li><a href="{{ path('client.ficheClient') }}"><span class="fi-torso"></span> Votre Profil</a></li>
            {% endif %}
            </ul>

        <ul class="right">
            {% if app.session.get('logged') is defined and app.session.get('logged') == 1 and app.session.get('droit') == 'admin' %}
                <li class="has-dropdown"><a href="#"><span class="fi-wrench"></span>Administration</a>
                    <ul class="dropdown">
                        <li><a href="{{ path('commande.adminList') }}"> Afficher les commandes </a></li>
                        <li><a href="{{ path('manifestant.add') }}"> Ajouter un Manifestant </a></li>
                        <li><a href="{{ path('manifestant.stock') }}"> Gestion des Stocks </a></li>
                        <li><a href="{{ path('client.ficheAllClient') }}">Gestion des Utilisateurs</a></li>
                    </ul>
                </li>
                <li class="divider"></li>

            {% endif %}
            {% if app.session.get('logged') is defined and app.session.get('logged') == 1 %}
                <li>
                    <a href="{{ url('client.logout') }}">Déconnexion</a>
                </li>
            {% else %}
                <li data-dropdown="login-dropdown" data-options="align:left">
                    <a href="#">Connexion</a>
                </li>
            {% endif %}
        </ul>
</section>
</nav>
<div id="login-dropdown" class="f-dropdown small content" data-dropdown-content="true">
    <form method="post" action="{{ url('client.login') }}">
        <input type="email" name="email" placeholder="Email du compte client">
        <input type="password" name="password" placeholder="Mot de passe">
        <input type="submit" value="Connexion" class="small button success">
    </form>
</div>
</div>