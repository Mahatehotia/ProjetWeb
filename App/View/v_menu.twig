<nav class="top-bar" data-topbar role="navigation">
    <ul class="title-area">
        <li class="name">
            <h1> <a href="{{ path }}index.php">WEB</a></h1>
        </li>
        <li class="toggle-topbar menu-icon">
            <a href="#"><span>Menu</span></a>
        </li>
    </ul>
    <section class="top-bar-section">
        <ul class="left">
            <li class="divider"></li>
            <li class="has-dropdown"><a href="#">Manifestant</a>

<ul class="dropdown">
    {% if   app.session.get('droit') is defined and  app.session.get('droit') == 'DROITadmin' %}
        <li><a href="{{ path('depense.add') }}"> créer une dépense </a></li>
    {% endif %}
    <li><a href="{{ path('depense.show') }}">afficher
            {% if   app.session.get('logged') is defined and  app.session.get('logged') == 1 %}
            {% if   app.session.get('droit') is defined and  app.session.get('droit') == 'DROITadmin' %}
            /editer/supprimer
            {% endif %}
            {% endif %}les dépenses</a></li>
</ul>
</li>

</ul>
<ul class="has-dropdown">
    {% if   app.session.get('logged') is defined and  app.session.get('logged') == 1 %}
        {% if   app.session.get('droit') is defined and  app.session.get('droit') == 'DROITadmin' %}
            <li class="has-dropdown"><a href="#">Dépenses</a>
                <ul class="dropdown">
                    <li><a href="{{ path('categorieDepense.add') }}"> créer une Catégories </a></li>
                    <li><a href="{{ path('categorieDepense.show') }}">afficher/editer/supprimer
                             les Catégories</a></li>
                </ul>
            </li>
        {% endif %}
</ul>
    <ul class="right">
        <li class="name" style="color: #d58512">Connecté en tant que : {{ app.session.get('login') }}</li>
        <li><a href="{{ path('user.logout')  }}">Se déconnecter</a></li> </ul>
    {% else %}
        <ul class="right">
        <li><a href="{{ path('user.login')  }}">Se connecter</a></li>
    {% endif %}
</ul>
</section>
</nav>